# Scoped podman config: lets upstream's Dockerfile use short base-image
# names (oven/bun, python) without prompting for registry resolution.
# Applied ONLY when launched through scripts/upstream-up.sh via the
# CONTAINERS_REGISTRIES_CONF env var - never installed globally.

unqualified-search-registries = ["docker.io"]
